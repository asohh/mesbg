dir=$(pwd)
sudo podman kill mesbg-db
sudo podman rm mesbg-db
sudo podman run -d --name mesbg-db -v $dir/data:/opt/couchdb/data -e COUCHDB_USER=admin -e COUCHDB_PASSWORD=mesbg  couchdb

sudo podman inspect mesbg-db | grep IPAddress
